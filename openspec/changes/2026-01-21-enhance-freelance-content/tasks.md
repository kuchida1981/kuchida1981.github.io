# タスク

1.  **設定の更新**
    - [x] `config.yml` を修正し、`menu.main` に「About」「Services」「Works」を追加する。
    - [x] `config.yml` の `params.profileMode.subtitle` を更新する。

2.  **Aboutページの作成**
    - [x] フロントマターと初期のプロフィールコンテンツを含む `content/about.md` を作成する。

3.  **Servicesページの作成**
    - [x] フロントマターとサービス内容の説明を含む `content/services.md` を作成する。

4.  **Worksセクションの作成**
    - [x] セクションを定義するために `content/works/_index.md` を作成する。
    - [x] プレースホルダーとしてサンプル実績記事 `content/works/sample-project.md` を作成する。

5.  **検証**
    - [x] ローカルでサイトをビルド (`hugo server`) し、ナビゲーションとコンテンツの表示を確認する。